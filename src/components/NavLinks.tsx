/**
 * Legacy NavLinks component - Now deprecated
 * Navigation links are now handled by NextNavbar component
 */
interface NavLinksProps {
  className?: string;
}

const NavLinks: React.FC<NavLinksProps> = ({ className }) => {
  // This component is deprecated. Navigation is now handled by NextNavbar.
  return null;
};

export default NavLinks;
